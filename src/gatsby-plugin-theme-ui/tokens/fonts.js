import { amstelvar, inter, dankMono } from "../font-faces"

const fonts = {
  body: inter[`@font-face`].fontFamily,
  heading: amstelvar[`@font-face`].fontFamily,
  code: dankMono[`@font-face`].fontFamily,
}

export default fonts
